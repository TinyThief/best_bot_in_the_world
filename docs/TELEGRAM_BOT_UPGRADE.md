# Telegram-бот: улучшения «как Шварценеггер на стероидах»

## Что уже есть

- Команды: /start, /help, /signal, /status, /db, /backtest_phases, /chart, /phases, /trend_daily, /id
- Reply-панель: Сигнал | БД | Мой ID | Помощь | Скрыть панель
- Inline под сообщениями: Обновить | БД (под сигналом) или Обновить | Сигнал (под БД)
- Меню бота (set_my_commands): все команды с коротким описанием

## Предлагаемые улучшения

### 1. Новые команды и срезы

| Команда | Описание | Эффект |
|--------|----------|--------|
| **/zones** | Только торговые зоны: ближайшая поддержка/сопротивление, текущая зона, перевороты ролей, confluence | Быстрый срез уровней без полного /signal |
| **/momentum** | Только импульс: состояние (сильный/затухающий/нейтральный), направление, RSI, return_5 по старшему ТФ | Срез перекупленности/перепроданности |
| **/health** | Свежесть БД по ТФ (последняя свеча), время последнего обновления, пинг Bybit (опционально) | Мониторинг «данные устарели» |

### 2. Обогатить /signal

- **Блок «Зоны»**: зона low–high, в зоне да/нет, у поддержки/сопротивления, до поддержки/сопротивления %; уровней с confluence ≥2.
- **Блок «Импульс»**: состояние (strong/fading/neutral), направление, RSI, return_5 по старшему ТФ.
- **Разбор score**: entry_score_breakdown (phase, trend, tf_align_ratio, stability_bonus) — одной строкой.

Так /signal остаётся единой «тяжёлой» сводкой, но с полной картиной (зоны + импульс).

### 3. Inline-кнопки и быстрые срезы

- Под сообщением «Сигнал»: **Сигнал** | **Зоны** | **Импульс** | Обновить (и при необходимости БД).
- По нажатию «Зоны» — ответ как от /zones (без повторного текста сигнала).
- По нажатию «Импульс» — ответ как от /momentum.

Удобно листать срезы без ввода команд.

### 4. Алерт при смене сигнала

- При смене **direction** (none → long/short или long ↔ short) бот сам шлёт короткое сообщение в чат (или в отдельный «алерт-чат» по конфигу).
- Опционально: только если confidence ≥ порога (например SIGNAL_MIN_CONFIDENCE или отдельный TELEGRAM_ALERT_MIN_CONFIDENCE).
- Реализация: хранить last_direction (и при необходимости last_confidence) в bot_data; при каждом тике или при вызове /signal/status сравнивать с текущим, при смене — отправить сообщение.

Варианты запуска проверки:
- В **job_queue** раз в N минут вызывать анализ и проверять смену (если бот только Telegram, без main.py).
- При **каждом /signal или /status** обновлять last_* и при смене слать алерт (проще, алерт только когда пользователь запросил обновление).
- Гибрид: отдельная job «тик раз в минуту» только для проверки смены и алерта (как в main.py, но внутри бота).

Рекомендация: алерт при смене вызывать из периодической job (раз в 1–2 мин), чтобы не зависеть от того, нажал ли пользователь «Обновить».

### 5. Улучшенный /help

- Кратко по блокам: «Сигнал и фазы», «Зоны и уровни», «Импульс», «Графики», «БД и мониторинг».
- Упомянуть кнопки: «Под сообщением — Сигнал / Зоны / Импульс / Обновить».

### 6. Дополнительно (по желанию)

- **/summary** — одна карточка: сигнал эмодзи + confidence + «у зоны»/«в зоне» + импульс (strong/fading) + одна строка причины. Для быстрого скриншота.
- **Форматирование**: жирный заголовок «Сигнал: 🟢 Long» (HTML или Markdown), списки по ТФ — читаемость.
- **Кнопка «Скрыть панель»** уже есть; при /start снова показать панель — уже реализовано.

---

## Итог приоритетов

1. **Реализовано**: /zones, /momentum, обогащён /signal (зоны + импульс + entry_score_breakdown), inline Сигнал | Зоны | Импульс | Обновить | БД, /health, обновлённый HELP (HTML-разметка).
2. **Реализовано**: алерт при смене direction — job раз в TELEGRAM_ALERT_INTERVAL_SEC (по умолчанию 90 с), отправка в TELEGRAM_ALERT_CHAT_ID при смене и confidence ≥ TELEGRAM_ALERT_MIN_CONFIDENCE. Включение: TELEGRAM_ALERT_CHAT_ID=chat_id, TELEGRAM_ALERT_ON_SIGNAL_CHANGE=1.
3. **По желанию**: /summary, жирные заголовки в остальных сообщениях.

После этого бот даёт полную аналитику (сигнал, зоны, импульс, здоровье БД) и быстрые срезы с кнопок — «как Шварценеггер на стероидах».
